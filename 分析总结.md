# jsound-openal 模组分析总结

> 基于 2024-08 源码通读整理（`src/` 全部 12 个 Java 文件 + 资源 + 构建产物）。

---

## 一、一句话定位

**把 Java Sound（`javax.sound.sampled`）音频路由到 LWJGL OpenAL 的 Minecraft 双端桥接模组**，
专为安卓启动器（**PojavLauncher / Zalith**）设计。

背景：安卓上没有桌面音频后端，JDK 的 `javax.sound.sampled` 只有类没有可用设备实现；
但 MC 本身的 OpenAL（LWJGL）可用。本桥让依赖 `javax.sound` 的音乐类模组
（注释里明确针对 **Concerto** 音乐播放器）在手机上正常出声。

---

## 二、基本信息

| 项目 | 内容 |
|---|---|
| Fabric 标识 | `fabric.mod.json`，id `jsound_openal`，version 1.0，`environment: *` |
| Forge 标识 | `META-INF/mods.toml`，modId `jsoundopenal`，loader `[47,)`（即 1.20.1），side CLIENT |
| 入口类 | Fabric: `com.jsound.openal.fabric.JSoundOpenalMod`；Forge: `com.jsound.openal.forge.JSoundOpenalForgeMod` |
| 构建产物 | `build/jsound-openal.jar`（约 31KB，双端入口 + SPI 声明都已打包） |
| 参考依赖 | `参考/lwjgl-glfw-classes.jar`（编译期参照的 LWJGL 类） |
| 自测入口 | `SinWaveSelfTest`（播放 2 秒 440Hz 正弦波冒烟测试） |
| License | Forge 侧声明 MIT |

---

## 三、架构与核心功能

### 1. SPI 注册（`JSoundMixerProvider` + `OpenALCore.registerFallback`）

- 通过 `META-INF/services/javax.sound.sampled.spi.MixerProvider` 标准注册。
- **关键技巧**：Fabric/Forge 类加载器不扫描 services 文件，所以用反射把自家
  Provider 直接塞进 `AudioSystem` 的私有 `mixers` 字段：
  - 幂等（已存在则跳过）；
  - 字段尚未初始化时直接"播种"，让首次 `getMixerInfo()` 就能看到本桥。
- 两个平台入口类（Fabric `ModInitializer.onInitialize` / Forge `@Mod` 构造器）都调用这个 fallback。

### 2. 独立 OpenAL 上下文（`OpenALCore`，单例）

- 自己 `alcOpenDevice` + `alcCreateContext` + `alcMakeContextCurrent`，
  **不复用游戏的上下文**，避免与 MC 渲染线程的音效引擎抢 OpenAL。
- 单一 worker 线程 `jsound-openal-worker` 持有上下文；**所有 AL 调用只在它上面跑**，
  mod 线程只投递任务 / 推 PCM / 置 volatile 标志。
  - `runWorkerSync`（10 秒超时）与 `register`（工厂在 worker 上创建 voice 并注册）。
- 失败降级安全：设备/上下文打开失败时 worker 空转，永不阻塞调用方。
- Voice 每 tick 检查，`tick()` 返回 false 即从注册表移除并 `free()` 释放 AL 资源。

### 3. Mixer 层（`JSoundMixer`）

- 暴露 `SourceDataLine` 与 `Clip` 两种 line；`maxLines = NOT_SPECIFIED`（多路并发）。
- 对**裸 `Line.Info`**（不带格式的枚举探测，Concerto 的询问方式）也返回支持。
- 格式过滤交给 `PCMFormat.supports`。

### 4. 播放与录制路径

**播放（source lines）**

| Line 类型 | Voice 实现 | 机制 |
|---|---|---|
| `Clip`（一次性短音频） | `ClipVoice` | 整段 PCM 一次性上传，单 source 播放；支持循环、字节偏移定位 |
| `SourceDataLine`（流式） | `StreamVoice` | 48 个 OpenAL buffer × 4096 帧（每个约 92ms）轮转填充；96 容量有界队列做**背压** |

流式路径要点：

- 队列满时 `write()` 阻塞——正好符合 Java Sound 的阻塞写契约；
- **多 chunk 打包填充**：把多个整 chunk 塞进同一个 OpenAL buffer（不完整的余量留队列），
  避免 buffer 太短导致 STOPPED/重播边界产生爆音；
- STOPPED 状态恢复：driver stop 后自动清队列，故无条件回收全部 buffer
  （注释记录了之前按 unqueue 回收导致泄漏死锁的坑）；
- flush / drain / underrun 检测、状态与守恒（idle+queued=48）诊断日志。

**录制（target lines，★ 语音输入）**

| Line 类型 | 实现 | 机制 |
|---|---|---|
| `TargetDataLine`（麦克风） | `OpenALTargetDataLine` | ALC11 捕获设备（`alcCaptureOpenDevice/Start/Samples`），每线一条捕获线程轮询 `ALC_CAPTURE_SAMPLES` |

捕获路径要点：

- 专属捕获线程拥有全部 ALC 调用；采样拉取上限 2048 帧/次，捕获缓冲 4096 帧；
- 捕获所得规范 16-bit LE 由 `PCMEncode` 反向编码成模组请求的变体
  （8-bit/大端/unsigned）再入队，与播放侧 `PCMConvert` 互为镜像；
- 有界队列（64 chunk）+ `read()` 阻塞语义：running 且无数据时等待，
  stop/close 后读完队列余量即返回；
- `flush()` 清队列 + 丢弃驱动侧已捕获样本；`available()` 只算已排队字节（读线程不做 ALC 调用）；
- Mixer 的 `getMaxLines(TargetDataLine) = 1`（安卓麦克风事实上独占）；
- **安卓前提**：启动器需授予麦克风权限（RECORD_AUDIO），否则
  `alcCaptureOpenDevice` 失败 → `LineUnavailableException`（多数设备仅 16-bit 单声道捕获）。

### 5. 音量控制（`LineControls`）

- 每条 Line 暴露 `MASTER_GAIN`（dB，-80 ~ +6.02）+ `MUTE`；
- dB → 线性：`10^(dB/20)`，MUTE 强制 0；
- **Concerto 特判**：音量 0 时它算出 `-Infinity`，被 clamp 到 -80dB 静音，
  而不是抛异常炸掉调用方播放线程。

### 6. 格式支持（`PCMFormat` + `PCMConvert`/`PCMEncode` 转换层）

- **入口接受面**：8-bit 或 16-bit、signed/unsigned、大端/小端、单声道/立体声，采样率不限；
- **内部统一**：播放侧非规范变体由 `PCMConvert` 在 open/write 时转换成
  signed 16-bit LE（8-bit 升位、大端换序、unsigned 偏置归零）再喂 OpenAL；
  录制侧由 `PCMEncode` 把捕获的规范 16-bit LE 反向编码回请求变体；
  `PCMFormat.of` 只接收转换后的规范格式，越界即抛 `IllegalArgumentException`（Java Sound 契约）；
- 流式路径的 `write()` 返回"输入字节"数，残留的半帧由转换器暂存到下次调用。

---

## 四、文件结构

```
src/main/java/com/jsound/openal/
├── OpenALCore.java             # 单例：OpenAL 设备/上下文 + worker 线程 + SPI 反射注册
├── JSoundMixerProvider.java    # SPI 入口（MixerProvider）
├── JSoundMixer.java            # 最小 Mixer 实现
├── OpenALSourceDataLine.java   # SourceDataLine 实现（流式写入，按需转换）
├── OpenALClip.java             # Clip 实现（整段上传、实时进度、有限循环）
├── StreamVoice.java            # 流式 voice（48 buffer 轮转 + 背压队列）
├── ClipVoice.java              # 一次性 voice（位置快照/软件循环/完成回调）
├── Voice.java                  # voice 接口（tick/free/setGain）
├── LineControls.java           # MASTER_GAIN + MUTE
├── PCMFormat.java              # 格式校验/归一化
├── PCMConvert.java             # ★ 格式转换层（8-bit/大端/unsigned → 16-bit LE）
├── PCMEncode.java              # ★ 反向编码层（捕获 16-bit LE → 请求变体）
├── OpenALTargetDataLine.java   # ★ TargetDataLine（ALC 捕获线程 + 阻塞 read）
├── Log.java                    # ★ 日志开关（-Djsound.openal.debug=true）
├── SinWaveSelfTest.java        # 正弦波冒烟测试
├── fabric/JSoundOpenalMod.java # Fabric 入口
└── forge/JSoundOpenalForgeMod.java # Forge 入口
src/main/resources/
├── META-INF/services/javax.sound.sampled.spi.MixerProvider
├── META-INF/mods.toml          # Forge 1.20.1
└── fabric.mod.json             # Fabric
build/jsound-openal.jar         # 已构建产物（Java 17 字节码，major 61）
build/_stubs/                   # 编译期 Fabric/Forge API 桩（仅编译用，不打包）
参考/lwjgl-glfw-classes.jar     # 编译参考（LWJGL OpenAL 类）
```

---

## 五、兼容性评估

### ✅ 大概率直接能用

典型音乐/音效模组写法几乎都被覆盖：

- 走 `AudioSystem.getSourceDataLine()` / `getClip()` 的
  （javazoom AudioPlayer、Concerto 这类）；
- 用**自带纯 Java 解码 SPI**（MP3SPI/VorbisSPI 等）把 MP3/OGG 解码成 16-bit PCM
  再喂给 Line 的；
- 任意采样率（校验只看编码/位深/声道，不卡 44100）；
- 用 `MASTER_GAIN`/`MUTE` 控音量的；
- 同开多路音频的；
- **语音输入/录音模组**（`AudioSystem.getTargetDataLine()`）——
  需启动器有麦克风权限；采样率/声道以设备驱动为准（多为 16-bit 单声道）。

### ❌ 肯定不行

| 情况 | 原因 |
|---|---|
| MIDI 音乐模组（`javax.sound.midi`，老模组重灾区） | 另一套 API，本桥完全没碰 |
| 直接往 Line 推非 PCM 编码流（裸 MP3/OGG/FLAC、24-bit、32-bit float） | 桥不带解码器，`open`/`write` 拒绝 |
| 依赖自带 Mixer / 指定其他 Mixer 名字的模组 | — |

### ⚠️ 能跑但退化 / 出错

| 问题 | 表现 | 状态 |
|---|---|---|
| `open(format, bufferSize)` | bufferSize 被忽略（一般无影响） | 未改 |
| `getLevel()` | 永远返回 0，频谱可视化没数据 | 未改 |
| Clip 循环区域 | `setLoopPoints` 的 loopEnd 不参与播放（循环区域恒为整段/从 loopStart 起） | 未改 |

> 以下曾列出的问题已在本次变更中修复：8-bit/大端/unsigned 拒收、大端播噪音、
> `Clip.loop(count)` 有限循环失效、Clip 假进度（详见第十节）。

### 失败方式是安全的

格式不支持时抛 `LineUnavailableException`，`AudioSystem` 继续问其他 Provider；
安卓上没有别的 Provider，模组报 "no line matching"——与没装本桥时表现一致，不会崩游戏。

---

## 六、老 mod 的三个高频坑（Java 17 时代 mod 基本都走 javax.sound）

1. **8-bit PCM 直接出局** —— 老模组爱用 8-bit 22kHz 小 WAV；桌面 `DirectAudioDevice`
   原生支持所以"桌面好好的"。→ **已修复**：`PCMConvert` 自动升到 16-bit。
2. **大端流播成噪音** —— AU/AIFF 大端不转换直接写。→ **已修复**：自动换序。
3. **裸编码流** —— `clip.open(mp3Stream)` 不带 MP3SPI 的写法桌面也响不了，
   不算桥的锅，但会遇到。→ 维持现状（需模组自带解码 SPI）。

---

## 七、已知问题与调试痕迹

- 原 `StreamVoice` 内大量 `System.err` 诊断输出（tick 状态、BALANCE 守恒检查、
  FILL 错误码、PLAY 失败追踪）→ **已收进 `Log` 门控**：
  周期性诊断默认静默，`-Djsound.openal.debug=true` 打开；错误路径（AL 错误、PLAY 失败、
  voice 崩溃）始终打印；上下文就绪保留一条 info。
- `open(format, bufferSize)` 的 bufferSize 仍被忽略；`getLevel()` 仍为 0；
  `setLoopPoints` 的 loopEnd 仍不参与播放。

---

## 八、改进清单执行情况

| # | 改进项 | 状态 |
|---|---|---|
| 1 | 轻量格式转换层（8-bit→16-bit、大端→小端、unsigned→signed） | ✅ 已实现（`PCMConvert`，20 项数值测试全过） |
| 2 | 调试日志降级/开关 | ✅ 已实现（`Log` + `jsound.openal.debug`） |
| 3 | Clip 实时进度（AL_BYTE_OFFSET 快照） | ✅ 已实现 |
| 4 | 有限次数 `loop(count)` 软件循环模拟 | ✅ 已实现 |
| 5 | 顺带修复：`start()` 不再意外无限循环（原 `loopStart==0` 时 loop=true） | ✅ |
| 6 | 顺带修复：Clip 自然播完发 STOP 事件并清 running | ✅ |
| 7 | 顺带修复：循环中的 Clip `close()` 确定性终止（原可能永不释放） | ✅ |
| 8 | 语音输入：`TargetDataLine`（ALC 捕获）+ 反向编码 `PCMEncode` | ✅ 已实现（19 项数值/往返测试全过；真机需麦克风权限） |

---

## 九、验证方法

- 跑 `SinWaveSelfTest`：应看到
  `[jsound-openal] bridge mixer present: true` → `got line: ...OpenALSourceDataLine` → 播放完成。
- 实机（Pojav/Zalith）看日志关键字：
  - 通了：`[jsound-openal] source line OPEN` →（开 debug 后）`STREAM PLAYING`；
  - 通了但卡顿：`UNDERRUN` / `STUCK` / `BALANCE` 不守恒；
  - 格式被拒：模组侧 "no line matching" / `Unsupported format`。
- 调试开关：给 JVM 加 `-Djsound.openal.debug=true`（Pojav/Zalith 可在 Java 参数里加）。

### 重新编译（本机已验证可用）

```sh
JVM=/data/data/com.dsharnessmobile.shell/files/usr/lib/jvm/java-17-openjdk
cd /storage/emulated/0/ide/jsound-openal
find src/main/java build/_stubs -name "*.java" > build/_sources.txt
$JVM/bin/javac -encoding UTF-8 -cp "参考/lwjgl-glfw-classes.jar" -d build/_out @build/_sources.txt
rm -rf build/_out/net                      # 剔除编译桩
cp -r src/main/resources/. build/_out/
$JVM/bin/jar --create --file build/jsound-openal.jar -C build/_out .
```
`build/_stubs` 里的 Fabric/Forge API 桩只用于编译，运行时由加载器提供。

---

## 十、本次变更记录（源码补丁）

**新增**

- `Log.java`：三级日志门。debug 由系统属性 `jsound.openal.debug` 控制（默认关），
  error 恒打，info 仅一次性生命周期行（如 `context ready`）。
- `PCMConvert.java`：任意受支持 PCM 变体 → signed 16-bit LE 的转换层。
  - `needsConversion` / `effectiveFormat` / `forFormat` / `convertAll` 静态入口；
  - 实例式流式转换：跨 `write()` 调用暂存半帧（carry）；
  - Clip 走 `convertAll` 一次性整段转换；StreamLine 走实例转换后分块入队；
  - `write()` 返回值折算回输入字节数。

**修改**

- `PCMFormat.supports`：位深放宽为 8/16-bit（大端/unsigned 同样放行）；
  `of()` 保持只收规范 16-bit LE（转换后才会到达）。
- `JSoundMixer.SUPPORTED`：广告列表补 8-bit 与大端条目（真实过滤仍在 `isLineSupported`）。
- `OpenALClip`：
  - open 路径接入转换（保留请求格式给 `getFormat()`，字节偏移用有效帧宽）；
  - `start()` 改为 `voice.start(false)`——原实现 `loopStart==0` 时传 loop=true，
    所有默认 Clip 会无限重播（bug）；
  - `loop(count)`：`LOOP_CONTINUOUSLY` 走 AL 原生循环，有限次数走软件重播；
  - `getLongFramePosition`/`getMicrosecondPosition` 改为实时位置
    （worker 每 tick 快照 `AL_BYTE_OFFSET`）；
  - 播放自然结束由 `ClipVoice.onFinish` 回调触发 STOP 事件并清 `running`；
  - `setFramePosition` 的偏移换算改用有效帧宽。
- `ClipVoice`：新增 `remainingLoops`/`lastByteOffset`/`onFinish`/`playedOnce`/
  `finishNotified` 状态；`close()` 同时置 `wantStop`（循环源可确定性终止）；
  抽出 `playFromOffset()` 供首播/重播/循环复用。
- `OpenALSourceDataLine`：open 时按格式决定是否挂转换器；`write()` 先转换后分块，
  返回输入字节数；`getBufferSize` 改用有效帧宽；日志全部走 `Log.debug`。
- `StreamVoice`：诊断日志全部降级为 `Log.debug`（AL 错误与 PLAY 失败保留 error）；
  移除 `fillLogs` 限频计数（debug 开关已覆盖）。
- `OpenALCore`：voice 移除/崩溃、上下文失败走 `Log`；`context ready` 保留单条 info。

**验证**

- `javac 17.0.20` 全量编译通过；jar 已重新打包（19 classes + 3 resources，无桩混入）。
- `PCMConvert` 20 项数值断言全过（8-bit u/s、大端换序、unsigned 偏置、半帧 carry、
  非对称字节端序）——其中大端用例曾在首版转换实现中抓出"双重换序"bug 并已修正。
- OpenAL 播放路径需真机验证：装进 Pojav/Zalith 后跑 `SinWaveSelfTest` 与实际音乐模组。

---

## 十一、语音输入变更记录（TargetDataLine）

**新增**

- `PCMEncode.java`：`PCMConvert` 的逆过程——把捕获的规范 signed 16-bit LE
  编码回模组请求的变体（8-bit u/s、16-bit 大端/unsigned）。无状态、线程安全。
- `OpenALTargetDataLine.java`：实现完整 `TargetDataLine`。
  - open：`ALC11.alcCaptureOpenDevice(null, rate, AL_FORMAT_MONO16/STEREO16, 4096)`；
    失败（如无麦克风权限）抛 `LineUnavailableException` 并提示权限原因；
  - 专属捕获线程（`jsound-openal-capture`，守护线程）拥有全部 ALC 调用：
    `alcCaptureStart` → 轮询 `ALC_CAPTURE_SAMPLES`（≤2048 帧/次）→
    `alcCaptureSamples` 拉取 → `PCMEncode` 编码 → 有界队列（64 chunk）；
    退出时 `alcCaptureStop`；`close()` join 线程后 `alcCaptureCloseDevice`；
  - `read()` 阻塞语义：running 且队列空时 `wait(100)`；stop/close 后读完余量返回；
    分块残段由 `pending/pendingPos` 承接；
  - `flush()` 清队列 + 丢弃驱动侧样本（线程活着走标志位，空闲时内联丢弃）；
  - `available()` 只统计已排队字节，读线程不做 ALC 调用（避免跨线程操作捕获设备）；
  - 位置统计按请求格式帧数（`bytesReadRequested / frameSize`）；
  - 无 controls；OPEN/START/STOP/CLOSE 事件齐全。

**修改**

- `JSoundMixer`：`getTargetLineInfo` 返回 `TargetDataLine` 信息；
  `isLineSupported`（裸 Info 与 DataLine.Info 两种路径）纳入 `TargetDataLine`；
  `getLine` 分发到 `OpenALTargetDataLine`；`getMaxLines(TargetDataLine) = 1`。

**测试**

- `PCMEncode` 19 项断言全过：needsEncoding 矩阵、8-bit u/s 边界
  （-32768→0/128、32512→255/127）、大端换序、unsigned 偏置（-1→0x7FFF、
  -32255→0x0201）、u8→16-bit→u8 往返逐字节一致。
- 排障记录：本机（安卓移植 OpenJDK）反射装箱的 `Boolean` 与字面量 `Boolean.TRUE`
  引用不等——首版测试用 `==` 比较 Boxed Boolean 误报失败，已改为先拆箱再比较；
  另修正两处测试期望值错误（unsigned 映射是 `raw = sample + 32768`）。

**真机验证要点**

- 给启动器授予麦克风权限（Pojav/Zalith 的应用权限里开 RECORD_AUDIO）；
- 走 `AudioSystem.getTargetDataLine(fmt)` → `open` → `start` → 循环 `read`；
- debug 开关下应看到 `target line OPEN/START`；失败信息会指明是权限还是驱动拒绝；
- 多数安卓设备仅支持 16-bit 单声道捕获，立体声请求可能在设备打开时被拒。

---

## 十二、流式沙沙声/断流修复（真机日志定位）

**现象**：歌曲开头 1~2 秒沙沙声；部分场景播放中断。测试模组 Concerto（自带
javazoom StreamPlayer），请求标准 `PCM_SIGNED 44100Hz 16bit stereo LE`（直通路径）。

**根因（诊断包全量日志 + 反编译 StreamPlayer 确认）**

1. **驱动怪癖**：该设备 OpenAL（安卓移植版）会把播完的 buffer 直接从
   `AL_BUFFERS_QUEUED` 中移除，而 **`AL_BUFFERS_PROCESSED` 永远为 0**。
   原补充循环只依赖 `processed`，导致前 48 个 buffer 播完后无法回收复用 →
   队列塞满 → `write()` 阻塞 → 只能靠"整体 STOPPED → 全量回收 → 重播"的
   4.45 秒大循环维持，且流式写入被节流到实时速率。
2. **饿肚开播**：`STREAM PLAYING queued=1`——第一个 23ms buffer 一到就开播，
   池子填满前 source 以几十 Hz 反复 停止→回收→重播，每次重启产生杂音突发
   （= 开头沙沙声）。
3. **非桥问题**：StreamPlayer 刷屏的 `Under run> Available=...` 只是日志
   （反编译确认不打断播放）；"后面没了"的一次是 Concerto 随**游戏暂停**
   （ESC/失焦）而暂停音乐，属 mod 自身行为。

**修复（`StreamVoice` 重构）**

- 回收改为驱动无关：`回收数 = max(processed, inflight − driverQueued)`，
  按 FIFO（`ArrayDeque inflight`）顺序拿回播完的 buffer ID；
  标准驱动走 `processed` 路径（照旧 unqueue），怪驱动走差值路径；
- **预缓冲门槛**：攒满 `PREBUFFER_BUFFERS=8`（约 0.74s）才开播/重播；
  生产者停供超 `STALL_PLAY_MS=300` 时照常播完尾段（歌曲结尾不吞字）；
- buffer 记账统一：`idlePool + inflight ≡ 48`，BALANCE 守恒恒成立。

**验证（诊断包日志）**

- `STREAM PLAYING queued=8`（预缓冲生效）；
- `queued=48 driver[queued=48]` 完全一致（修复前 48 vs 44/26…），队列持续消化，
  `write()` 不再死锁；
- BALANCE 违例 0；3 条 `STUCK queue.size=1` 为歌曲结尾最后一帧的单 tick 瞬态，
  由尾段规则自愈，无害；
- 用户实听确认沙沙声消失、播放连续。

**日志门**：诊断期 `Log.DEBUG` 曾默认开启，正式包已恢复
`-Djsound.openal.debug=true` 门控；`OPEN` 行保留 info 级（每线一条）。
正式包 md5 `fd93b31c…` 已部署全部 4 个版本目录。
